// 小屠适配接口

// mod atm;
// mod disaster;
// mod elevator;
// mod environment;
// mod instrument;
// mod vehicle;

/*
/// 传感器消息信息
struct SensorMsg
{
    /// 消息ID
    id: SensorMsgId,

    /// 消息全局ID
    guid: Uuid,

    /// 传感器状态
    state: SensorState,

    /// 传感器上的读数
    reading: f64,

    /// 发生时间
    time: TimePoint,

    /// 截图URL
    image: String,

    /// 传感器信息
    sensor: SensorInfo,

    /// 视频源信息
    source: SourceInfo,
    inner: NetOutputInfo,
}

/// 传感器信息
struct SensorInfo
{
    /// 传感器ID
    id: SensorId,
    /// 传感器全局ID
    guid: Uuid,
    /// 传感器类型
    type_: SensorType,
    /// 名称
    name: String,
    /// 传感器参数
    params: SensorParams,
    ///Calendar	CalendarTrigger	工作表目前没看出存在的必要	O

    /// 关联视频源Id
    source_id: SourceId,
    source_guid: Uuid,
    /// 允许状态
    enabled: bool,
}

/// 传感器参数信息
struct SensorParams
{
    /// 型号
    model: String,
    /// 图片上的感兴趣区域
    roi: RoiD,
    /// 灵敏度
    sensitivity: i32,
    /// 取值范围
    range: RangeD,
}


*/
